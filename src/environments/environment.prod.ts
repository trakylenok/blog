import { Environment } from "./interface";

export const environment: Environment = {
  production: true,
  apiKey: 'API key', //your API key here
  fbDBUrl: 'https://database-link' //your firebase database link
};
